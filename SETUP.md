# Setup Instructions

## Initial Setup

1. Install dependencies:
```bash
npm install
```

2. Run the development server:
```bash
npm run dev
```

3. Open [http://localhost:3000](http://localhost:3000) in your browser.

## Project Status

✅ All pages implemented
✅ Components created
✅ Design system configured (Gold, Burgundy, Cream theme)
✅ YouTube video integration ready
✅ Forms prepared (email integration pending)

## Next Steps

1. **Add Real Assets**: Replace placeholder crown emoji with actual Queen E avatar and logo
2. **Configure Email**: Set up email service for:
   - Ask Queen E form (`components/QuestionForm.tsx`)
   - Newsletter signup (`components/NewsletterForm.tsx`)
3. **Add Gallery Images**: Place images in `public/images/` and update `data/gallery.ts`
4. **Add Social Media Links**: Update footer and contact page with real social media URLs
5. **Add More Episodes**: Update `data/episodes.ts` with additional YouTube videos

## Deployment to Vercel

1. Push code to GitHub
2. Import project in Vercel
3. Deploy automatically

The project is ready for deployment!

